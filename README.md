# 🧠 K-Nearest Neighbors (KNN) from Scratch

Реализация алгоритма **k ближайших соседей (KNN)** с нуля на чистом Python без использования готовых ML-библиотек для обучения.
Проект охватывает полный ML-пайплайн: от разведочного анализа данных (EDA) до обучения модели, оценки качества и визуализации границ принятия решений на датасете **Iris**.

---

## 🎯 Цели проекта

1. Загрузить и исследовать датасет Iris
2. Провести разведочный анализ данных (EDA)
3. Реализовать алгоритм KNN «с нуля» с использованием NumPy
4. Обучить модель и оценить её качество
5. Сравнить реализацию с `scikit-learn`
6. Визуализировать границы принятия решений в 2D-пространстве

---

## 📁 Структура проекта

```
project/
│
├── src/
│   └──  knn.py              # Реализация KNN с нуля
│
├── metrics/
│   └── classification.py       # Метрики оценки качества
│
├── notebooks/
│   └── exploration.ipynb   # EDA + обучение + визуализация
│
├── requirements.txt
├── README.md
└── .gitignore
```

---

## 🛠️ Технологии и зависимости

**Python 3.9+**

**Основные библиотеки:**

* **NumPy** — численные вычисления
* **Pandas** — обработка и анализ данных
* **Matplotlib / Seaborn** — визуализация
* **Scikit-learn** — загрузка датасета, разбиение выборки

```txt
# requirements.txt
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
jupyter>=1.0.0
```

---

## 📈 Особенности реализации

### KNN (from scratch)

* Евклидово расстояние между объектами
* Поддержка многоклассовой классификации
* Голосование по `k` ближайшим соседям
* Гибкая настройка параметра `k`
* Отсутствие этапа обучения (lazy learning)

### Метрики

* Реализована функция `accuracy_score`
* Поддержка сравнения с `sklearn.neighbors.KNeighborsClassifier`

---

## 🧪 Разведочный анализ данных (EDA)

В рамках EDA выполнено:

* Анализ структуры и качества данных
* Проверка баланса классов
* Анализ распределений признаков
* Попарный анализ признаков (`pairplot`)
* Анализ корреляций
* Визуальная оценка разделимости классов

EDA показал, что признаки, связанные с **лепестками**, обладают наибольшей информативностью для классификации.

---

## 🏆 Результаты

На датасете Iris получены следующие результаты:

| Метрика                         | Значение   |
| ------------------------------- |------------|
| **Accuracy (KNN from scratch)** | **≈ 1.00** |
| **Accuracy (sklearn KNN)**      | **≈ 1.00** |
| **Разница**                     | Идентично  |

Результаты собственной реализации полностью сопоставимы с реализацией из `scikit-learn`.

---

## 📊 Визуализации

Проект включает следующие визуализации:

1. Распределение классов
2. Boxplot признаков по классам
3. Pairplot признаков
4. Матрицу корреляций
5. Scatter plot для признаков лепестков
6. **Границы принятия решений KNN в 2D-пространстве**

Визуализация границ решений выполнена по двум наиболее информативным признакам:

* `petal length (cm)`
* `petal width (cm)`

---

## 🚀 Итог

Проект демонстрирует:

* глубокое понимание алгоритма KNN
* умение реализовывать ML-алгоритмы с нуля
* корректную работу с данными и визуализациями
* соответствие промышленному стилю ML-проектов
